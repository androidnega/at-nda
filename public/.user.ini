; Raise PHP upload limits for APK distribution (cPanel / PHP-FPM).
; Changes can take a few minutes to apply after deploy.
upload_max_filesize = 128M
post_max_size = 132M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M

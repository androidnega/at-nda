; Document root (public/) — primary location for cPanel per-directory PHP limits.
; Allow up to 50 MB APK uploads. Changes may take up to 5 minutes to apply.
upload_max_filesize = 50M
post_max_size = 55M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M

; Document root (public/) — primary location for cPanel per-directory PHP limits.
; Allow up to 500 MB APK uploads. Changes may take up to 5 minutes to apply.
upload_max_filesize = 500M
post_max_size = 520M
max_execution_time = 600
max_input_time = 600
memory_limit = 512M

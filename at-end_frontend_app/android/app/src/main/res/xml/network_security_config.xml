<?xml version="1.0" encoding="utf-8"?>
<!--
    Release-build network security policy.

    - cleartextTrafficPermitted="false" globally: HTTP is rejected by
      OkHttp / HttpURLConnection / the Dart `http` package out of the
      box. Production API base URL is https://at-enda.manuelcode.info,
      so there is no legitimate cleartext destination in release.
    - System trust anchors only: no user-installed CAs, which prevents
      a hostile root CA on the device from MITM'ing API traffic with a
      malicious profile.

    The debug build overrides this file (see
    `src/debug/res/xml/network_security_config.xml`) to allow cleartext
    against the Android emulator's host loopback (10.0.2.2) so a
    dart-define override of API_BASE_URL to http://10.0.2.2:8000/api
    still works during local development.

    Reference: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

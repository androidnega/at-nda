<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- =====================================================================
         Permissions we ACTUALLY use. Every entry below has a corresponding
         runtime feature in the Flutter app. Keep this list minimal — Play
         Console reviewers compare it against the app's described function
         and disclosed Data Safety section.

         Justification map (kept here so the next maintainer can update
         the Play Console Data Safety form without re-deriving it):
           INTERNET            — talk to Laravel API (mark attendance, sync)
           ACCESS_NETWORK_STATE — connectivity_plus, debounced sync triggers
           POST_NOTIFICATIONS  — flutter_local_notifications (foreground-only,
                                 session-start / mark-success tickets)
           VIBRATE             — haptic feedback after a successful mark
           CAMERA              — QR scanning + face verification capture
           ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION — geofence
                                 attendance validation
           USE_BIOMETRIC       — local_auth fingerprint / Face gate before
                                 a mark is POSTed
    ===================================================================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- =====================================================================
         Permissions that Flutter plugins ADD by default but we DO NOT use.
         Removing them keeps the merged manifest minimal so Play does not
         ask awkward "why does an attendance app want this?" questions and
         Data Safety stays honest.

         tools:node="remove" tells the manifest merger to drop these even
         though a transitive AAR declares them.
    ===================================================================== -->

    <!-- Advertising id — we do NOT serve ads. Required by Play Data
         Safety: apps that include AD_ID and target API 33+ MUST declare
         advertising data collection, which we have none of. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <!-- We only show immediate notifications (mark confirmation, session
         live). We never schedule future alarms, so these restricted
         permissions are not needed. SCHEDULE_EXACT_ALARM and
         USE_EXACT_ALARM are Play-restricted on Android 13+ and require
         a special declaration we should not be making. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        tools:node="remove" />

    <!-- We do not record audio. The camera plugin adds RECORD_AUDIO for
         the video-recording surface area; we only capture stills for the
         face-verification flow. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"
        tools:node="remove" />

    <!-- Storage — we never read the user's gallery. All captures are
         held in memory or sent straight to the server. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"
        tools:node="remove" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:node="remove" />

    <!-- Legacy biometric permission, replaced by USE_BIOMETRIC on API 28+.
         The local_auth plugin still references it for backwards
         compatibility on API 23–27. We are minSdk 23 so we keep neither
         the explicit declaration nor the merged one — USE_BIOMETRIC is
         enough for the devices Play accepts. -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT"
        tools:node="remove" />

    <!-- =====================================================================
         Hardware features. Marking these required="false" widens the set
         of devices that can install the app (e.g. tablets without a
         back camera). The runtime gracefully handles unsupported
         features (the QR scanner page falls back to manual code entry).
    ===================================================================== -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <application
        android:label="at-enda"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Package visibility (Android 11+). We only use the platform's
         PROCESS_TEXT intent (Flutter engine reads this for the text
         selection menu). We do NOT launch external URLs, dial numbers,
         or compose mail, so we keep this list minimal. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
